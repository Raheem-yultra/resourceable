import { BrowseExperience } from '@/components/search/BrowseExperience';

// Unified marketplace browse, type toggle defaults to "All" (plan §5).
export default function BrowsePage() {
  return <BrowseExperience title="Browse ResourceAble" syncUrl />;
}
